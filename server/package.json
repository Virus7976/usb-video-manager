{
  "name": "usb-video-manager-server",
  "version": "0.1.0",
  "private": true,
  "description": "Local HTTP backend for USB/SD Auto-Action — serves the face review and questions to a phone on the LAN.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "fastify": "^5.2.1"
  },
  "engines": { "node": ">=20" }
}
